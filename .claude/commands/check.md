# 빌드·린트 검증

프로젝트의 코드 품질을 한 번에 점검한다.

## 실행 순서

1. `pnpm lint` 실행
2. lint 실패 시 에러 내용 분석 후 수정 방안 제시 (자동 수정 가능하면 `pnpm lint:fix` 실행)
3. `pnpm build` 실행 (TypeScript 타입 체크 + Vite 빌드)
4. build 실패 시 타입 에러 분석 후 수정 방안 제시

## 출력

- 모두 통과: "lint ✅ · build ✅" 한 줄 요약
- 실패 항목이 있으면: 에러 원인과 수정 방안을 간결하게 정리
